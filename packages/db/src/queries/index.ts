// Query helpers that are the database's business rather than a router's —
// anything where getting it wrong is a correctness bug, not a preference.
export * from "./dealNumber";
export * from "./defaultStages";
